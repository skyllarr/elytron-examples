<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>elytron-quickstart</groupId>
    <version>1.0-SNAPSHOT</version>
    <artifactId>server</artifactId>
    <packaging>war</packaging>

    <properties>
        <maven.compiler.release>11</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <version.org.wildfly.security.wildfly-elytron>2.2.2.Final</version.org.wildfly.security.wildfly-elytron>
        <version.org.wildfly.bom.wildfly-ee-with-tools>29.0.1.Final</version.org.wildfly.bom.wildfly-ee-with-tools>
        <version.jakarta.jws.jakarta.jws-api>3.0.0</version.jakarta.jws.jakarta.jws-api>
        <version.org.codehaus.mojo.exec-maven-plugin>3.1.0</version.org.codehaus.mojo.exec-maven-plugin>
        <version.org.wildfly.tools.wildfly-maven-plugin>4.2.0.Final</version.org.wildfly.tools.wildfly-maven-plugin>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.wildfly.bom</groupId>
                <artifactId>wildfly-ee-with-tools</artifactId>
                <version>${version.org.wildfly.bom.wildfly-ee-with-tools}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.wildfly.security</groupId>
            <artifactId>wildfly-elytron</artifactId>
	    <version>${version.org.wildfly.security.wildfly-elytron}</version>
        </dependency>
        <dependency>
            <groupId>jakarta.jws</groupId>
            <artifactId>jakarta.jws-api</artifactId>
	    <version>${version.jakarta.jws.jakarta.jws-api}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>ws-client-integration</finalName>
        <plugins>
            <!-- WildFly plug-in to deploy the WAR. This allows us to use mvn wildfly:deploy -->
            <plugin>
                <groupId>org.wildfly.plugins</groupId>
                <artifactId>wildfly-maven-plugin</artifactId>
		<version>${version.org.wildfly.tools.wildfly-maven-plugin}</version>
            </plugin>

            <!-- This attaches compiled classes so they can be used in the client -->
            <plugin>
                <artifactId>maven-war-plugin</artifactId>
		<version>${version.org.codehaus.mojo.exec-maven-plugin}</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                    <attachClasses>true</attachClasses>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
